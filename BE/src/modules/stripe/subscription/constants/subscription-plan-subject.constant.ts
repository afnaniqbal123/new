/**
 * The subject name for subscription plan records in authorization rules.
 *
 * Declared by the module that owns the data, not by the authorization module —
 * see `src/modules/authorization/README.md`.
 */
export const SUBSCRIPTION_PLAN_SUBJECT = 'SubscriptionPlan';
